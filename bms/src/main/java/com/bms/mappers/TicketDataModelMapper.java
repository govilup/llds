package com.bms.mappers;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface TicketDataModelMapper {

    //@Mapping(source = "ticket.id", target = "ticketId")
    //TODO: mappings
    //BookTicketResponseDto entityToDto(Ticket ticket);
}
